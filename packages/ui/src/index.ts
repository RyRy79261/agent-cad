/**
 * Shared UI primitives for the control panel.
 *
 * SCAFFOLD: this package is intentionally minimal. The functional UI spec
 * (docs/ui-functional-spec.md) is handed to the design pass, which populates
 * this package with shadcn/ui components (Button, Card, Dialog, Tabs, ...).
 */

export { cn } from "./lib/cn.js";
